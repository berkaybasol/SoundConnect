package com.berkayb.soundconnect.modules.event.performer.enums;

/** A public performer link and publication on its own calendar are separate permissions. */
public enum EventPerformerRequestPurpose {
	PERFORMER_CONSENT,
	PROFILE_VISIBILITY
}
